# Scraping
playwright>=1.40.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
brotli>=1.1.0
fake-useragent>=1.4.0

# Data processing
openpyxl>=3.1.2
phonenumbers>=8.13.0
email-validator>=2.1.0

# CRM export (optional — only needed for --format mysql)
pymysql>=1.1.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0

# AI (optional — only needed if ai_fallback_enabled=True and ai_provider=anthropic)
anthropic>=0.39.0
